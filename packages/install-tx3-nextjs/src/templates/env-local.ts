export const envLocalTemplate = `# TX3 Protocol Configuration
# Update these values according to your TX3 setup

# TX3 TRP (Transaction Request Protocol) Endpoint
NEXT_PUBLIC_TRP_ENDPOINT="http://localhost:8164"

# TX3 API Key (optional, leave empty if not required)
NEXT_PUBLIC_TRP_API_KEY=""
`;